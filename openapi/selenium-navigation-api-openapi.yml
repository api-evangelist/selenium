openapi: 3.2.0
info:
  title: Selenium WebDriver (W3C Wire Protocol) Navigation API
  description: Selenium WebDriver implements the W3C WebDriver protocol — a platform- and language-neutral HTTP wire protocol used by out-of-process programs to remotely instruct browsers.
  version: '1.0'
  contact:
    name: Selenium
    url: https://www.selenium.dev/documentation/webdriver/
servers:
- url: http://localhost:4444/wd/hub
  description: Default Selenium standalone / Grid endpoint
- url: http://localhost:9515
  description: ChromeDriver default port
- url: http://localhost:4444
  description: Geckodriver / generic WebDriver default
tags:
- name: Navigation
  description: Page navigation and metadata
paths:
  /session/{sessionId}/url:
    parameters:
    - $ref: '#/components/parameters/SessionId'
    post:
      tags:
      - Navigation
      summary: Navigate to a URL
      operationId: navigateTo
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - url
              properties:
                url:
                  type: string
                  format: uri
      responses:
        '200':
          description: OK
    get:
      tags:
      - Navigation
      summary: Get the current page URL
      operationId: getCurrentUrl
      responses:
        '200':
          description: Current URL
          content:
            application/json:
              schema:
                type: object
                properties:
                  value:
                    type: string
                    format: uri
  /session/{sessionId}/title:
    parameters:
    - $ref: '#/components/parameters/SessionId'
    get:
      tags:
      - Navigation
      summary: Get page title
      operationId: getTitle
      responses:
        '200':
          description: Title
  /session/{sessionId}/back:
    parameters:
    - $ref: '#/components/parameters/SessionId'
    post:
      tags:
      - Navigation
      summary: Navigate back
      operationId: navigateBack
      responses:
        '200':
          description: OK
  /session/{sessionId}/forward:
    parameters:
    - $ref: '#/components/parameters/SessionId'
    post:
      tags:
      - Navigation
      summary: Navigate forward
      operationId: navigateForward
      responses:
        '200':
          description: OK
components:
  parameters:
    SessionId:
      in: path
      name: sessionId
      required: true
      schema:
        type: string
      description: WebDriver session ID
